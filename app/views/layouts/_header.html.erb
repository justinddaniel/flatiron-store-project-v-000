<nav class="">
    <ul>
        <li> <%= link_to "Store Home", store_path %> </li>
        <% if session[:user_id] %>
          <li> Signed in as <%= User.find_by(:id => session[:user_id]).email %> </li>
          <li> <%= link_to "Sign out", signout_path %></li>
        <% else %>
          <li> <%= link_to "Sign In", signin_path %></li>
          <li> <%= link_to "Sign Up", signup_path %></li>
        <% end %>
    </ul>
</nav>
